# AGENTS.md — 项目操作约束与决策原则

本文件记录 BTC_5MIN 项目在运行策略评估、参数调整、纸面/实盘切换时必须遵守的硬性约束。任何 AI agent 或操作者在给出建议或修改配置前应先阅读本文件。

## 1. 项目概述

Polymarket BTC 5 分钟涨跌市场交易机器人。单进程 `python main.py` 并行启动纸面基线、实盘交易（可选）和仪表盘。

- 入口：`python main.py`
- 仪表盘：`http://127.0.0.1:8787/`
- 日志：`logs/live_orders.csv`（实盘）、`logs/paper/<tf>/paper_trades.csv`（纸面）、`logs/live_session_state.json` / `logs/paper/<tf>/session_state.json`（持久累计 PnL 与运行态）
- 配置：`.env.dashboard`（dashboard 写回此文件），缺省值见 `config.py`
- 测试：`python -m pytest -q`

## 2. 策略归属约束（重要）

### 2.1 一个策略同一时间只能存在于"纸面"或"实盘"其中一种模式

由 `config.py` 中的 `_exclude_strategy_ids` 强制：

- `.env.dashboard` 的 `PAPER_STRATEGY_IDS` 与 `LIVE_STRATEGY_IDS` 互斥
- 当某 strategy_id 同时声明在两侧时，实盘侧优先，运行时会自动从纸面集合中剔除
- 当前的状态：`LIVE_STRATEGY_IDS=7,11`、`PAPER_STRATEGY_IDS=1,2,3,4,5,6,8,9,10,12,13`

推论：
- **不存在"先在纸面调好→再上实盘"的工作流**。一旦策略挪到实盘，纸面就停止运行该策略，无法做并行对照验证。
- 评估"参数调整是否合理"无法靠同步纸面实验。只能依靠：
  1. 历史 CSV 日志的回测模拟（不依赖运行中的纸面）
  2. 实盘已有的成交数据
  3. 等待新数据积累

### 2.2 纸面与实盘共用同一套策略参数

来自 `.env.dashboard.example` 第 63 行项目规则以及 README 的说明：

```
# 只支持 STRATEGY_<id>_<参数名>，纸面和实盘共用同一套策略参数。
```

虽然代码里有 `PAPER_STRATEGY_<id>_*` / `LIVE_STRATEGY_<id>_*` profile 覆盖机制，但本项目操作约定要求**不为同一策略设置两侧不同的参数**。当前 `.env.dashboard` 中没有任何 `PAPER_STRATEGY_11_*` 或 `LIVE_STRATEGY_11_*` 覆盖项，符合此约定。

推论：
- 任何 `STRATEGY_<id>_*` 参数改动都会同时影响实盘和纸面（只要该策略在哪一侧运行）
- 调整实盘策略 11 的参数 = 也调整了"如果将来回到纸面"的参数，反之亦然

## 3. 策略评估的统计意义门槛

**严格禁止基于小样本做出调参决策。** 既往实例：策略 11 实盘 10 笔（5 胜 5 负），曾被我错误解读为"严重背离纸面"，但 10 笔二项分布在 50% 附近完全在随机波动范围内，不构成统计证据。

决策门槛：

| 数据量 | 适用结论 |
|--------|----------|
| < 20 笔成交 | 任何胜率/PnL 差异都可视为噪声，禁止调整参数 |
| 20–50 笔 | 仅可用于趋势观察，仍不足以触发参数改动 |
| 50–100 笔 | 方可讨论方向性调整，建议继续观察 |
| > 100 笔 | 才具备统计意义上的调参依据 |

## 4. 改参决策前的强制诊断步骤

在建议修改 `STRATEGY_<id>_*` 任何门槛参数前，必须做以下诊断，避免误诊：

1. **查清"瓶颈在哪一关"**：用 `Import-Csv logs/live_orders.csv` 统计每个 `skip_reason` 的占比。
   - 例如：策略 11 近 2 日全部 SKIP，其中 82% 是 `strategy11_probability_too_low`，12.5% 是 `strategy11_edge_too_low`，说明瓶颈在 probability 信号本身太弱，而不是 edge 门槛过严。
2. **量化"改这个参数能救回多少成交"**：
   - 算出被 skip 的样本里有多少 `signal_<field>` 落在"当前门槛 → 新门槛"区间内。
   - 若新增 < 5 笔，则调整基本无效，不要修改。
3. **检查信号本身是否在状态**：
   - 若 raw 信号（如 `signal_probability`）长期贴近下限附近（如 0.50 ± 0.01），说明市场状态不适合该策略。放松门槛等于把噪声当信号，会改变策略本质。
4. **确认改动会同时影响实盘和纸面**（见 §2.2），不能"只改纸面先试"。

## 5. 实盘 vs 纸面表现不完全可比

纸面成交是按理论价撮合的；实盘要经过 FOK 下单、滑点、手续费、`live_fok_not_filled` 等失败路径。两侧差异是结构性而非异常：

- 实盘胜率低于纸面是正常现象
- 实盘订单 `order_cost` 含 `fee`，纸面不扣 fee（部分历史版本）
- 实盘有 `strategy_*_price_too_high` / `live_fok_not_filled` 等独有 SKIP 原因

因此不能用"实盘 vs 纸面胜率差"作为调参的充分理由。

## 6. 怎样才允许调整参数

最少满足以下任一条件，且通过 §4 诊断：

- 单策略实盘成交 ≥ 100 笔且累计 PnL 为负或最大回撤超过单笔 stake 的 10 倍
- 业务逻辑发生变化（例如交易品种、最小单位调整，市场结构改变）
- 已有 ≥ 200 笔的回测模拟（独立 Python 脚本基于 `logs/live_orders.csv` 或 `data/history_*.csv`），覆盖不同市场状态

满足后：
- 一次只改一个参数
- 改动幅度不超过当前值的 25%
- 同步更新 `.env.dashboard`，禁止用临时环境变量绕过持久配置
- 通过 `python -m pytest -q` 保持测试绿
- 不要 commit 私钥/凭证相关项

## 7. 关键文件清单

| 路径 | 用途 |
|------|------|
| `config.py` | 所有参数默认值 + profile 装配逻辑 |
| `.env.dashboard` | 运行配置（dashboard 写回） |
| `.env.dashboard.example` | 配置注释模板 |
| `logs/live_orders.csv` | 实盘逐轮记录（含 SKIP 原因和信号字段） |
| `logs/paper/<tf>/paper_trades.csv` | 纸面逐轮记录 |
| `logs/live_session_state.json` | 累计 PnL、连亏、pending 订单 |
| `strategy_decision.py` | 各策略信号决策入口 |
| `models.py` | `LiveStrategyState` 等数据结构 |
| `state_manager.py` | 状态持久化字段白名单 |
| `main.py` | 唯一受支持的运行入口 |
| `docs/operations_runbook.md` | 日常运维手册 |
| `docs/strategy7-5m-parameter-playbook.md` | 策略 7 参数调优手册 |

## 8. Git 操作约束

- 提交前先运行 `python -m pytest -q`
- 不要提交 `.env.dashboard` 中的 `POLYMARKET_PRIVATE_KEY` / `POLYMARKET_API_KEY` / `POLYMARKET_API_SECRET` / `POLYMARKET_API_PASSPHRASE` 等凭证
- `.gitignore` 已包含关键凭证项，不要为图便利移除
- 用户未明确要求时不要 `git commit` / `git push`

## 9. 当前运行态快照（最近更新：2026-07-01）

- 实盘：`LIVE_STRATEGY_IDS=7,11`
- 纸面：`PAPER_STRATEGY_IDS=1,2,3,4,5,6,8,9,10,12,13`
- `TRADE_MODE=both`
- 策略 7 累计 cash_pnl +17.88，无连续亏损，状态健康
- 策略 11 累计 cash_pnl +0.41，实盘 10 笔 5 胜 5 负，样本严重不足，不调整
- 策略 10 仅在纸面，累计 +80.50 但最大回撤 82.53，继续观察暂不上实盘

## 10. 策略评估深度记录

### 10.1 策略 7（实盘，主策略）

**全历史**（截至 2026-07-01）：
- 实盘 69 笔，胜率 56.5%，累计 cash_pnl +17.88
- 全历史最大连亏 5 笔（发生在 2026-06-23 那波），当前 0 连亏
- 最大回撤 35.23（cash_pnl 最高 +27.60 → 最低 -7.64），占当时仓位的很大比例

**近期趋势向上**：
- 最近 30 笔：胜率 60%，PnL +15.09
- 最近 20 笔：胜率 70%，PnL +25.51
- 近 2 日只成交 2 笔（全胜 +6.83），SKIP 大量 `strategy7_ofi_too_weak` / `entry_too_late`，`ofi_threshold=0.58` + `momentum_threshold=0.008` + `max_entry_price=0.54` 起到强过滤作用

**风险点**：6/23 那波 5 连亏是前车之鉴，不要轻易放松入场条件去增加交易频率。

**建议**：保持现状。可考虑把 `MAX_CONSECUTIVE_LOSSES` 从 7 降到 5-6，让止损更早介入（6/23 连亏 5 笔损失惨重，之后花了一周才回血）。

### 10.2 策略 11（实盘，低活跃）

**关键事实**：
- 实盘 10 笔 5 胜 5 负，累计 cash_pnl +0.41，最大回撤 16.10
- 纸面 19 笔 16 胜 3 负，累计 +15.67，最大回撤 14.47
- 实盘最近一次成交：2026-06-25，已停 6 天
- 近 2 日实盘 305 条记录全部 SKIP，纸面同期 303 条也全部 SKIP（**不是实盘特有问题，是信号处于静默期**）
- SKIP 分布：82% `probability_too_low`（signal_probability 卡在 0.50 附近），12.5% `edge_too_low`
- 当前 `.env.dashboard` 实盘配置：`MIN_EDGE=0.025`、`MIN_PROBABILITY=0.52`、`BASE_ORDER_COST=2`、`ENTRY_TIMING=PRE_CLOSE`
- 纸面 vs 实盘成交时段不重合（纸面 06/02-06/23，实盘 06/10-06/25），两侧样本不可直接比较

**诊断结论**：
- 信号本身在 0.5 锚点附近几乎不动，市场状态不适合该策略
- 真正瓶颈是 `probability` 信号太弱而非 `edge` 门槛过严
- 实盘全期 1017 条 `edge_too_low` 中，只有 2 条 `edge∈[0.015,0.020)` 和 4 条 `edge∈[0.020,0.025)` 落在可救区间，改 edge 救不了多少成交
- 放松 `min_probability` 等同抛硬币，会把噪声当信号，改变策略本质

**建议**：保持原配置不动。等待 BTC 进入有明确方向性的市场状态（信号 prob 主动突破 0.52 自然就会下单），同时积累到至少 30-50 笔实盘样本再做下一步评估。

### 10.3 策略 10（仅在纸面，"漂亮"有水分）

**全历史**（截至 2026-07-01）：
- 纸面 531 笔，胜率 55.9%，累计 cash_pnl +80.50
- 最大回撤 82.53（cash_pnl 从未跌破 0，最低 -2.03 → 最高 80.50 一直在涨）
- 最大连亏 7 笔
- 日成交 20-45 笔，是高频策略

**本质上是策略 7 的"概率包装版"**：
核心公式（`strategy_decision.py:257-261`）：
```
fair_up = base_up + 0.08·ofi_score + 1.0·momentum_delta
edge = fair_up - up_price - edge_buffer
```
由于 Polymarket 正常情况 `up_price + down_price ≈ 1.0`，`base_up ≈ up_price`，所以：
```
edge ≈ 0.08·ofi + momentum_delta - 0.005
典型的 edge >= 0.04 要求就是 0.08·ofi + momentum_delta >= 0.045
```
OFI 权重只有 momentum 的 8%（`ofi_weight=0.08` vs `momentum_weight=1.0`），OFI 信号几乎不起作用。所谓"概率定价"只是把 `momentum + 0.08·ofi` 套了一层概率乘除的外壳，本质上仍是"等 BTC 动了再追方向"。

**"+80.50" 主要来自一个窄区间**：

| edge 区间 | 笔数 | 胜率 | PnL |
|----------|------|------|-----|
| [0.04, 0.06) | 154 | **51.3%** | **-3.91** |
| [0.06, 0.10) | 193 | **61.7%** | **+68.47** |
| [0.10, 0.20) | 52 | 55.8% | +6.89 |
| [0.20+) | 5 | 80% | +5.39 |

84% 的利润都来自 `edge∈[0.06, 0.10)` 这一个区间。`[0.04, 0.06)` 这一段反而亏，是纯拖累。说明当前 `MIN_EDGE=0.04` 太低——把"信号实际上很弱"的样本也放进来。

**疑点**：
1. "追入"逻辑的实盘适应性差：每笔都等动量已发生再入场，纸面按理价拍；实盘 FOK 下单时做市商可能已撤单，填充率会低，即便填充价格也已被推高到无利可图
2. 回撤 82.53 = 41×单笔 stake，比策略 7（35×）按比例风险更大
3. OFI 权重过小表明"多信号合成"几乎是摆设
4. 顺 vs 反 momentum 胜率接近（顺 57.7% vs 反 59.0%），反的还略高——说明"动量追入"的因果性其实弱

**改参空间**（理论上）：
- 把 `STRATEGY10_MIN_EDGE` 从 0.04 调到 0.06 能剔除负贡献段，预期 PnL 从 +80 涨到 +84
- 但按 §6 "改动幅度不超过当前值的 25%"，一次只能 0.04 → 0.05（25% 调幅），不能跳到 0.06（50%）

**当前建议**：暂不调整。
- 策略 10 只在纸面，不构成实盘风险，没必要急改
- 改之前还要验证：`[0.04, 0.06)` 那 154 笔是不是**整体阶段特征**而不是均匀分布的负 alpha。建议把这段按时间分组看分布。
- 策略 10 真正的问题不在门槛，而在"动量追入"的实盘适应性。建议先做一次 FOK 模拟回测（用 `logs/live_orders.csv` 中策略 7 的 `live_fok_not_filled` 频率估算策略 10 如果上实盘的失败率），再决定是否调参。